# iftechio-todo-vue 项目说明文档

## 1.技术选型

- **编程语言**: JavaScript，理由：生态成熟、开发效率高、前端开发标准语言，拥有丰富的工具链和社区支持。
- **框架/库**: Vue 3，理由：轻量级、响应式系统高效，Composition API提供了更好的逻辑复用和TypeScript支持，组件化开发便于维护和扩展。
- **UI组件库**: Element Plus，理由：专为Vue 3设计，组件丰富、文档完善、社区活跃，提供现代化的UI设计，显著提升开发效率。
- **构建工具**: Vite，理由：开发服务器启动速度极快、热更新效率高、构建速度快，提供了优秀的开发体验。
- **数据存储**: localStorage，理由：轻量级、无需额外依赖、适合小型应用的数据持久化，用户数据仅保存在本地，无需后端服务。
- **替代方案对比**:
  - **框架对比**: 相比React，Vue的API设计更简洁直观
  - **数据存储对比**: 相比IndexedDB，localStorage实现更简单但存储容量较小；相比服务端数据库，无需后端支持但无法实现多设备同步。

## 2.项目结构设计

- **整体架构说明**: 该项目采用前端单页应用架构，基于Vue3 Composition API实现，采用组件化和组合式函数设计模式。整个应用为纯前端应用，数据存储在浏览器的localStorage中，无需后端服务支持。应用架构遵循关注点分离原则，将UI组件、业务逻辑和数据存储分离。

- **目录结构示例**:

  ```
  src/
    components/        # 组件目录
      todo/            # 待办事项相关组件
        TodoForm.vue   # 待办事项列表组件
        TodoDialog.vue # 待办事项添加/编辑对话框组件
    composables/       # 组合式函数目录
      useStorage.js    # 存储相关的组合式函数
      useTodos.js      # 待办事项业务逻辑的组合式函数
    App.vue            # 应用主组件，包含搜索功能和主布局
    main.js           # 应用入口文件，初始化Vue应用
  ```

- **模块职责说明**:
  - **App.vue**: 应用的根组件，负责整体布局和搜索功能，将搜索状态传递给子组件。
  - **components/todo/TodoForm.vue**: 负责展示待办事项列表，处理待办事项的标记完成、编辑和删除操作。
  - **components/todo/TodoDialog.vue**: 提供待办事项的添加和编辑表单，包含表单验证逻辑。
  - **composables/useTodos.js**: 核心业务逻辑模块，提供待办事项的增删改查功能和搜索逻辑。
  - **composables/useStorage.js**: 提供本地存储的封装，实现数据的持久化保存和读取。
  - **main.js**: 应用程序的入口点，负责创建Vue应用实例、注册全局组件和插件。

## 3.需求细节与决策

- **描述是否必填？如何处理空输入？**
  - 标题为必填项，描述为可选项。系统通过表单验证确保标题不为空，当用户尝试提交空标题时，会显示错误提示并阻止提交。对于描述字段，允许为空，系统会将空描述视为空字符串处理。

- **已完成的任务在 UI 中如何显示？**
  - 在UI中，已完成的任务通过不同样式与未完成任务区分，主要表现为：文本添加删除线样式、背景色变浅、任务标题文字颜色变为灰色，同时任务前的复选框显示为勾选状态。

- **任务排序逻辑**
  - 系统默认按照任务创建时间进行排序，新创建的任务显示在列表底部。

- **已实现的扩展功能**
  - **数据本地存储**：通过封装localStorage实现了数据的持久化存储，确保页面刷新或浏览器重启后数据不丢失。系统使用自定义的useStorage组合式函数管理存储逻辑，提供了统一的数据读写接口。
  - **搜索功能**：实现了按任务标题和描述的搜索功能。用户可通过顶部搜索框输入关键词，然后点击搜索按钮进行筛选，提升了数据查找效率。

## 4.AI使用说明

- **使用AI工具 Trae**
- **使用AI的环节**
  - 待办事项，本地存储的代码片段生成
  - 新增表单的Bug定位
  - 文档初稿的编写
- **AI输出的修改**
  - AI通过localStorage来实现对待办事项的增删改查，我改成了通过useTodos组合式函数进行封装，将业务逻辑与存储逻辑分离，提高了代码的可维护性和复用性。

## 5.运行与测试方式

- **本地运行方式**
  - 安装依赖

  ```sh
  npm install
  ```

  - 启动命令

  ```sh
  npm run dev
  ```

- **已测试过的环境**
  - **操作系统**: Windows 11
  - **Node.js版本**: Node.js v22
  - **浏览器**: Edge 90+

- **已知问题与不足**
  - **存储限制**: 使用localStorage存储，存在约5MB的存储容量限制
  - **多设备同步**: 由于仅使用本地存储，不支持数据在多设备间的同步
  - **性能优化**: 在任务数量较多（超过1000条）时，可能会出现页面渲染性能下降

## 6.总结与反思

- **如果有更多时间，我会将任务的排序可选为按优先级进行排序**

- **我认为这个项目的最大亮点是，采用了Vue 3组合式API的设计模式，通过useStorage和useTodos两个自定义Hook实现了存储逻辑与业务逻辑的解耦。这种设计不仅使代码结构清晰、易于维护，还提高了逻辑复用性，符合现代前端工程化的最佳实践。**
